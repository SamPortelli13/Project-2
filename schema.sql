CREATE TABLE "usa_ufo" (
    country_id INTEGER NOT NULL,
    summary VARCHAR,
    city VARCHAR,
    state VARCHAR,
    shape VARCHAR,
    duration VARCHAR,
    city_latitude FLOAT8,
    city_longitude FLOAT8,
    date VARCHAR,
    time VARCHAR
);